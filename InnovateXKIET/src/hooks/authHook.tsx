// // hooks/useAuth.ts
// import { useSelector } from 'react-redux';

// // Define a type for the state if you have one
// interface RootState {
//   userReducer: {
//     auth: boolean;
//     // Add other fields if necessary
//   };
// }

// export const useAuth = () => {
//   const { auth } = useSelector((state: RootState) => state.userReducer);
//   return auth;
// };
